setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    web: Dockerfile_Deploy

release:
  command:
    - php /var/www/artisan migrate --force
  image: momocm

run:
  web: sh /var/www/entrypoint.sh